{
  "name": "bashalagbe",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.41.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.2.1",
    "express": "^4.18.3",
    "express-rate-limit": "^6.10.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^8.17.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.3.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm install && npm run build",
    "render-build": "npm install && cd frontend && npm install && npm run build",
    "start": "node server.js",
    "dev": "npm run dev --prefix frontend & npm run dev --prefix backend",
    "setup": "npm run setup:env && npm install && cd backend && npm install && cd frontend && npm install && npm run build",
    "setup:env": "node -e \"const fs = require('fs'); if (fs.existsSync('backend/.env')) { fs.copyFileSync('backend/.env', '.env'); console.log('✅ Environment variables copied'); } else { console.log('❌ backend/.env not found'); }\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mahirTaj/BashaLagbe.git"
  },
  "keywords": [],
  "author": "",
  "bugs": {
    "url": "https://github.com/mahirTaj/BashaLagbe/issues"
  },
  "homepage": "https://github.com/mahirTaj/BashaLagbe#readme",
  "description": ""
}

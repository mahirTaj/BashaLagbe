{
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "frontend/build",
  "framework": "create-react-app",
  "ignoreCommand": "echo 'Deploying always'",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://YOUR-BACKEND-DOMAIN.onrender.com/api/:path*" }
  ]
}
